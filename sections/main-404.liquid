<div class="page-width empty-state">
  <h1>{{ 'templates.404.title' | t }}</h1>
  <p style="opacity:.7">{{ 'templates.404.subtext' | t }}</p>
  <a href="{{ routes.root_url }}" class="button">{{ 'general.continue_shopping' | t }}</a>
</div>

{% schema %}
{
  "name": "404 principal",
  "settings": []
}
{% endschema %}
